import React from "react";

const style = {
  padding: 20,
  fontSize: 20,
  fontWeight: "bold",
  backgroundColor: "#ede8f6"
};

const Header = () => (
  <header style={style}>
    <h1>Pokedex</h1>
  </header>
);

export default Header;
